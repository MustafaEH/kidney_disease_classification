import 'package:flutter/material.dart';

class ShowImageSourceDialog extends StatelessWidget {
  const ShowImageSourceDialog({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
